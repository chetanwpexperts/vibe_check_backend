#Database Connection URL (used by app/database.py)
#Note: 'vibe_db' is the service name defined in docker-compose.yml
#In production, this would be the public host/IP.
DATABASE_URL="postgresql+asyncpg://vibe_user:strong_password@localhost:5432/vibe_check"